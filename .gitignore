*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Never commit signing material — losing or leaking the upload key is unrecoverable.
*.jks
*.keystore
keystore.properties

# Agent tooling installed per-machine, not part of the app.
/.agents/
/.claude/
/skills-lock.json
